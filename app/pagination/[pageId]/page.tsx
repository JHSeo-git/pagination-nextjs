type PaginationPageProps = {
  searchParams: { pageId?: string };
};

function PaginationPage({ searchParams }: PaginationPageProps) {
  return <div>PaginationPage</div>;
}

export default PaginationPage;
